
const enum Common {
  PARENT = 'h-screen w-screen max-w-screen max-h-screen bg-slate-300 text-slate-950 flex flex-col gap-3 text-xl'
}

export const enum Mobile {
  PARENT = `${Common.PARENT}`
}

export const enum Desktop {
  PARENT = `${Common.PARENT}`
}
